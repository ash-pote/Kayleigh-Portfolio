const swiper = new Swiper('.swiper', {
    speed: 400,
    spaceBetween: 0,
    loop: true,
    autoplay: {
        delay: 6000,
        disableOnInteraction: false,
      },
    
    // If we need pagination
    // pagination: {
    //      el: '.swiper-pagination',
    // },
  });